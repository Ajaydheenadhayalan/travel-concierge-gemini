# ✈️ Travel Concierge AI Agent

A simple AI-powered Travel Planning Agent that generates itineraries, suggests hotels, estimates cost, and refines plans based on user instructions. Built using a multi-agent system and powered by Gemini.

## 🚀 Features
* **Generate full multi-day itineraries**
* **Recommend hotels within budget**
* **Estimate total trip cost**
* **Refine plans using natural language** (e.g., “make it cheaper”)
* **Clean JSON output**
* **Backend (FastAPI) + Frontend (React)**

## 🧠 Agents Used
* **Planner Agent** – creates and merges itinerary
* **Search Agent** – finds attractions
* **Hotel Agent** – recommends stays
* **Pricing Agent** – calculates cost
* **Refinement Agent** – modifies existing plans
* **Coordinator Agent** – manages workflow & memory
